name: Validate Schemas & Examples

on:
  push:
    branches: ["main", "develop"]
  pull_request:
    branches: ["main"]

jobs:
  validate:
    name: JSON Schema Validation
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Validate positive examples against schemas
        run: make validate

      - name: Run negative validation tests (must-fail examples)
        run: make validate-negative

      - name: Check JSON formatting (prettier)
        run: make lint
